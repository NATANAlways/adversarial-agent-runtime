# TIMELOG.md

## Pre-Part-A setup (outside the 6-hour Part A budget)

The take-home lists `mockllm/` under "What you're given" — it's meant to be
provided infrastructure, not something scored as part of R1-R8. It wasn't
present in this environment, so it had to be built before Part A could
start. Logged separately and honestly excluded from the 6-hour clock, since
none of this is the agent runtime itself.

| Date | Task | Duration | Notes |
|---|---|---|---|
| 2026-07-27, ~22:10-22:30 IST | Built `mockllm/`: `server.py` (Messages-API-shaped HTTP server, scenario engine for S1-S12), `tokenizer.py` (deterministic token counter), 12 `scenarios/*.yaml`, `README.md`, smoke test suite (16 tests), top-level `Makefile` | ~20 min | Built with AI (Claude Code) assistance. Duration estimated from file write timestamps — doesn't include my own reading/review time, so treat as a lower bound. |

## Part A (6-hour cap starts here)

| Date | Task | Duration | Notes |
|---|---|---|---|
| | | | |

<!-- Add one row per work session. Keep entries honest, including dead ends
     and requirements you deferred -- a gap explained in one line here is
     worth more than a suspiciously clean log. -->
